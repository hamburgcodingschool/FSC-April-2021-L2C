// Use the AND operator and the % operator.

// Ask the user for their age;
// Calculate their birthYear;
// Check if the birthYear is a leap year: 
// exactly divisible by 4 and NOT divisible by 100

// year % 4 === 0 AND year % 100 !== 0